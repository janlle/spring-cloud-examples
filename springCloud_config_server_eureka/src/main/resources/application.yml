server:
  port: 9001

eureka:
  client:
    serviceUrl:
      defaultZone: http://user:password123@localhost:8761/eureka
spring:
  application:
    name: foobar
  cloud:
      config:
        server:
          git:
            uri: https://gitee.com/thethink/config_repo