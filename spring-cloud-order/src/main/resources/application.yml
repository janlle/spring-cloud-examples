server:
  port: 9002

spring:
  application:
    name: mc-order

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    registry-fetch-interval-seconds: 10
    fetch-registry: false